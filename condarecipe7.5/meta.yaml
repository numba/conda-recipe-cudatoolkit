package:
   name: cudatoolkit
   version: 7.5

build:
  number: 3

requirements:
  build:
    - python >=3
    - requests
    - 7za # [win]
    - conda
    - pyyaml

source:
    path: ../scripts/

test:
  requires:
    - numba
